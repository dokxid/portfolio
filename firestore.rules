rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /video/{document=**} {
      allow read: 
        if request.auth.token.role == "privileged" 
          || request.auth.token.role == "admin"
      allow write: 
        if false
    }
  }
}